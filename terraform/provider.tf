terraform {
  required_providers {
    supabase = {
      source  = "supabase/supabase"
      version = "~> 1.0"
    }
  }
}

provider "supabase" {
  # access_token can be set via SUPABASE_ACCESS_TOKEN env var
}
