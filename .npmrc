# Use modern flag for production installs (avoids "Use --omit=dev instead" warning).
# Deploy/build: run `npm install --omit=dev` (or `npm run install:prod`) instead of `npm install --production`.
# Local dev: run `npm install` as usual (devDependencies will be installed).
